/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: adapters
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CustomAttributeOptionPostApiModel from './CustomAttributeOptionPostApiModel';
import CustomAttributeType from './CustomAttributeType';

/**
 * The GlobalCustomAttributePostApiModel model module.
 * @module model/GlobalCustomAttributePostApiModel
 * @version 1.0.0
 */
class GlobalCustomAttributePostApiModel {
    /**
     * Constructs a new <code>GlobalCustomAttributePostApiModel</code>.
     * @alias module:model/GlobalCustomAttributePostApiModel
     * @param name {String} Name of attribute
     * @param type {module:model/CustomAttributeType} Type of attribute
     */
    constructor(name, type) { 
        
        GlobalCustomAttributePostApiModel.initialize(this, name, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, type) { 
        obj['name'] = name;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>GlobalCustomAttributePostApiModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GlobalCustomAttributePostApiModel} obj Optional instance to populate.
     * @return {module:model/GlobalCustomAttributePostApiModel} The populated <code>GlobalCustomAttributePostApiModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GlobalCustomAttributePostApiModel();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('isEnabled')) {
                obj['isEnabled'] = ApiClient.convertToType(data['isEnabled'], 'Boolean');
            }
            if (data.hasOwnProperty('isRequired')) {
                obj['isRequired'] = ApiClient.convertToType(data['isRequired'], 'Boolean');
            }
            if (data.hasOwnProperty('options')) {
                obj['options'] = ApiClient.convertToType(data['options'], [CustomAttributeOptionPostApiModel]);
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], CustomAttributeType);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GlobalCustomAttributePostApiModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GlobalCustomAttributePostApiModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GlobalCustomAttributePostApiModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['options']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['options'])) {
                throw new Error("Expected the field `options` to be an array in the JSON data but got " + data['options']);
            }
            // validate the optional field `options` (array)
            for (const item of data['options']) {
                CustomAttributeOptionPostApiModel.validateJSON(item);
            };
        }

        return true;
    }


}

GlobalCustomAttributePostApiModel.RequiredProperties = ["name", "type"];

/**
 * Name of attribute
 * @member {String} name
 */
GlobalCustomAttributePostApiModel.prototype['name'] = undefined;

/**
 * Indicates whether the attribute is available
 * @member {Boolean} isEnabled
 */
GlobalCustomAttributePostApiModel.prototype['isEnabled'] = undefined;

/**
 * Indicates whether the attribute value is mandatory to specify
 * @member {Boolean} isRequired
 */
GlobalCustomAttributePostApiModel.prototype['isRequired'] = undefined;

/**
 * Collection of attribute options   Available for attributes of type `options` and `multiple options` only
 * @member {Array.<module:model/CustomAttributeOptionPostApiModel>} options
 */
GlobalCustomAttributePostApiModel.prototype['options'] = undefined;

/**
 * Type of attribute
 * @member {module:model/CustomAttributeType} type
 */
GlobalCustomAttributePostApiModel.prototype['type'] = undefined;






export default GlobalCustomAttributePostApiModel;

